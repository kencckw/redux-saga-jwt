{
    "name": "redux-saga-jwt",
    "version": "1.0.2",
    "description": "Token management for single page application",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "repository": {
        "type": "git",
        "url": "https://github.com/kencckw/redux-saga-jwt.git"
    },
    "bugs": {
        "url": "https://github.com/kencckw/redux-saga-jwt/issues"
    },
    "homepage": "https://github.com/kencckw/redux-saga-jwt",
    "scripts": {
        "test": "jest",
        "test:cover": "jest --coverage",
        "coveralls": "cat ./coverage/lcov.info | coveralls",
        "lint": "tslint {src}/**/*.{ts,tsx}",
        "build": "rm -Rf ./dist && tsc && cp -rf package.json ./dist && cp -rf README.md ./dist && cp -rf LICENSE.md ./dist"
    },
    "keywords": [
        "typescript",
        "npm",
        "redux",
        "react",
        "redux-saga"
    ],
    "author": "Ken Cheng",
    "license": "MIT",
    "devDependencies": {
        "@types/jest": "^22.2.3",
        "@types/node": "^10.10.1",
        "coveralls": "^3.0.2",
        "dot-json": "^1.0.4",
        "jest": "^22.4.4",
        "ts-jest": "^22.4.6",
        "tslint": "^5.11.0",
        "tslint-react": "^3.6.0",
        "tslint-react-recommended": "^1.0.15",
        "typescript": "^3.0.3"
    },
    "dependencies": {
        "redux": "^4.0.0",
        "redux-saga": "^0.16.0"
    }
}
